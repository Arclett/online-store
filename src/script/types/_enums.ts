export enum FilterType {
    category = "category",
    brand = "brand",
}

export enum RangeFilters {
    price = "price",
    stock = "stock",
}

export enum CartParams {
    page = "page",
    limit = "limit",
}
